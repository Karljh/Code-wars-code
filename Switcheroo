using System;
using System.Linq;

public class Kata
{
  public static string Switcheroo(string )
  {
    var s = x.Select(a=> a == 'a' ? 'b' : (a== 'b' ? 'a' : a)).ToArray();
    x = new String(s);
    
    return x;
  }
}
